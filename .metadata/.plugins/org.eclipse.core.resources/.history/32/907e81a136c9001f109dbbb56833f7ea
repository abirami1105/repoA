<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pet Clinic - Register Pet</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #e3f2fd;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #0288d1;
            color: white;
            text-align: center;
            padding: 20px 0;
        }

        header h1 {
            font-size: 2.5rem;
            margin: 0;
        }

        header p {
            margin: 10px 0 0;
            font-size: 1.2rem;
        }

        .container {
            max-width: 600px;
            margin: 2rem auto;
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .container h2 {
            text-align: center;
            color: #0288d1;
            font-size: 1.8rem;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        label {
            font-weight: bold;
            margin-bottom: 5px;
        }

        input[type="text"] {
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        input[type="text"]:focus {
            border-color: #0288d1;
            outline: none;
        }

        input[type="submit"] {
            padding: 10px;
            font-size: 1.2rem;
            background: #0288d1;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.3s ease-in-out;
        }

        input[type="submit"]:hover {
            background: #0277bd;
        }

        .link {
            text-align: center;
            margin-top: 20px;
        }

        .link a {
            color: #0288d1;
            text-decoration: none;
            font-weight: bold;
            border: 2px solid #0288d1;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background 0.3s ease-in-out, color 0.3s ease-in-out;
        }

        .link a:hover {
            background: #0288d1;
            color: white;
        }
    </style>
</head>
<body>
    <header>
        <h1>Pet Clinic</h1>
        <p>Register Your Pet and Schedule Consultation</p>
    </header>

    <div class="container">
        <h2>Register Pet</h2>
        <form action="/api/pets/" method="post">
            <label for="PetName">Pet Name</label>
            <input type="text" id="PetName" name="PetName" placeholder="Enter Pet's Name" required />

            <label for="Breed">Breed</label>
            <input type="text" id="Breed" name="Breed" placeholder="Enter Pet's Breed" required />

            <label for="OwnerName">Owner's Name</label>
            <input type="text" id="OwnerName" name="OwnerName" placeholder="Enter Owner's Name" required />

            <label for="ConsultationFee">Consultation Fee</label>
            <input type="text" id="ConsultationFee" name="ConsultationFee" placeholder="Enter Consultation Fee" required />

            <input type="submit" value="Register Pet" />
        </form>

        <div class="link">
            <a href="/api/pets/">View All Pets</a>
        </div>
    </div>
</body>
</html>
